# Python 3.10+ recommended (3.9 is EOL as of October 2025)
# If you must use Python 3.9, keep importlib-metadata pinned

# Core dependencies
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0

# Google Cloud dependencies
google-cloud-bigquery>=3.11.0
google-cloud-storage>=2.10.0
db-dtypes>=1.1.0
pydata-google-auth>=1.8.0

# Authentication
google-auth>=2.20.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# Fix for Python 3.9 importlib.metadata issue (remove if using Python 3.10+)
importlib-metadata>=4.13.0,<5.0.0

# Optional: Add these if you have SSL/urllib3 warnings
# urllib3>=1.26.0,<2.0.0

# Optional: Ensure latest setuptools for pkg_resources deprecation
setuptools>=65.0.0