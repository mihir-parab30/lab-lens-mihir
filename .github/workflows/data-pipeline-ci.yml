name: Data Pipeline CI

on:
  pull_request:
    branches:
      - main
    paths:
      - 'data_preprocessing/**'
      - 'src/utils/**'
  push:
    branches:
      - main
    paths:
      - 'data_preprocessing/**'
      - 'src/utils/**'
  workflow_dispatch:

jobs:
  test-preprocessing:
    name: Test Data Preprocessing
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov
          pip install -r requirements.txt

      - name: Run preprocessing tests
        run: |
          pytest data_preprocessing/tests/ -v --cov=data_preprocessing --cov-report=xml

      - name: Test preprocessing pipeline
        run: |
          python data_preprocessing/scripts/main_pipeline.py --help || true

      - name: Validate imports
        run: |
          python -c "from data_preprocessing.scripts.preprocessing import MIMICPreprocessor; print('✅ Preprocessing imports OK')"
          python -c "from data_preprocessing.scripts.validation import MIMICDataValidator; print('✅ Validation imports OK')"

  lint-preprocessing:
    name: Lint Data Preprocessing
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install linting tools
        run: |
          pip install flake8 black isort

      - name: Check code formatting
        run: |
          black --check data_preprocessing/
          isort --check-only data_preprocessing/

      - name: Lint code
        run: |
          flake8 data_preprocessing/ --max-line-length=127 --extend-ignore=E203,W503
